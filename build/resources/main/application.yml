spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petfinity
    username: capstone
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #show_sql: true
        format_sql: true

logging:
  level:
    root:
    org.hibernate.SQL: debug
    #org.hibernate.orm.jdbc.bind: trace

server:
  port: 8080
  address: 0.0.0.0